# forge_calculator

A Python calculator tool.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate  # or .venv\Scripts\activate on Windows
pip install -e .
```

## Usage

(TODO — describe what this calculator computes.)
