[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "forge-calculator"
version = "0.1.0"
description = "A Python calculator tool."
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["forge_calculator*"]
